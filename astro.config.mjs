// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://venkateshwar-reddy-jambula.github.io',
  trailingSlash: 'never',
  build: { format: 'directory' },
});
